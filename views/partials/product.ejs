<div class="container">

    <div class="row">
        <div class="col-12">
            <div class="card " >
                <div class="row">
                    <div class="col-4">
                        <img src="/img/<%= urun.imgUrl  %>" class="img-fluid" alt="...">
                    </div>

                    <div class="card-body col-8">
                        <h5 class="card-title">
                            <%= urun.name %>
                        </h5>

                        <p> ÜRÜN ADI: <%= urun.name %>
                        </p>
                        <p> ÜRÜN FİYATI:<%= urun.price %>
                        </p>

                        <% if (urun.isActive) { %>
                            <p> ürün satışta</p>
                            <% } else { %>
                                <p> ürün satışta değil</p>
                                <% } %>
                                    <br><br>
                                   
                                    <p class="card-text">Some quick example text to build on the card title and make up
                                        the bulk of
                                        the card's
                                        content.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div><a class="btn btn-primary mt-2" href="/products">GERİ</a></div>

</div>